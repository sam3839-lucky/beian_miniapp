# 备案价小程序 v2 — 设计系统

## Product Context

- **产品**：深圳一手房备案价查询微信小程序
- **用户**：深圳购房者（刚需+改善），25-40 岁，手机端为主
- **场景**：购房决策辅助——查备案价、看去化率、比楼盘、算房贷
- **竞品**：贝壳找房、链家、安居客
- **差异化**：备案数据（权威）、去化率（独家）、取证日期（时效）、楼栋级数据（颗粒度）

## Aesthetic Direction

- **方向**：数据实用主义（Data-first Utilitarian）
- **装饰级别**：极简 — 字体排版承担全部视觉层次
- **调性**：可信、透明、高效。像 Bloomberg 终端尊重数据密度，但在手机端保持可读性。
- **参照**：贝壳找房（中国房产 App 设计基准）、链家

## Typography

- **策略**：系统原生字体栈，零网络加载，微信小程序原生兼容
- **字体**：`-apple-system, BlinkMacSystemFont, "PingFang SC", "Helvetica Neue", sans-serif`
- **理由**：小程序不支持 `@font-face` 外部加载。PingFang SC 是 iOS/macOS 中文屏显最优解，Helvetica Neue 覆盖西文数字。

### 字阶

| 大小 | 粗细 | 用途 |
|------|------|------|
| 10px | 400-500 | 标签、辅助文字、取证日期 |
| 12px | 400-600 | 正文、按钮、列表项、位置 |
| 14px | 600-700 | 子标题、卡片标题 |
| 16-18px | 700 | 重要数值、单价 |
| 20-24px | 800 | 卡片级价格 |
| 32px | 800 | 页面级主价格（详情页 hero） |

### 价格数字
价格统一用 `font-weight: 800` + `letter-spacing: -0.5px` 突出。不依赖 `font-variant-numeric: tabular-nums`（Android 小程序不支持）。

## Color

- **策略**：克制 — 1 个主色 + 3 个语义色。颜色在数据中才有意义。

### 色板

| 角色 | 色值 | 用途 |
|------|------|------|
| Primary | `#07C160` | 主按钮、选中态、品牌标识、链接 |
| Text Primary | `#1A1A1A` | 标题、项目名、价格（暗色模式下反转为 `#E5E5E5`） |
| Text Secondary | `#666` | 位置、标签、元信息 |
| Text Hint | `#999` | 辅助文字、占位符 |
| Background | `#F5F5F5` | 页面底色 |
| Card | `#FFFFFF` | 卡片背景 |
| Border | `#EEE` / `#F0F0F0` | 分割线、卡片边框 |

### 语义色

| 角色 | 色值 | 用途 |
|------|------|------|
| Red | `#FF4D4F` | 高去化率（≥80%）、价格、错误 |
| Orange | `#FF8C00` | 中去化率（10%-80%）、警告、快速组合按钮 |
| Blue | `#3B82F6` | 链接、信息 |

### 使用规则
- 去化率 < 10%：灰色（`#CCC` 进度条，`var(--text-hint)` 文字）
- 去化率 10%-80%：橙色（`#FF8C00` 进度条，`#D97706` 文字）
- 去化率 ≥ 80%：红色（`#FF4D4F` 进度条，`#E53E3E` 文字）
- 绿色仅用于品牌元素和交互态，不作为数据色

## Spacing

- **基础单位**：8px
- **密度**：紧凑

| Token | 值 | 用途 |
|-------|-----|------|
| xs | 4px | chip 内边距、图标间距、迷你进度条 |
| sm | 8px | 卡片内留白、同行元素间距 |
| md | 12px | 卡片外边距、筛选区段间距 |
| lg | 16px | 区域间距、页面 padding |
| xl | 20px | 大区块间距、页面顶部留白 |

### 信息密度目标
- 列表页首屏可见 2.5-3 张项目卡片（375×812 视口）
- 每张卡片高度约 130px（含去化率条）

## Layout

- **策略**：网格优先 — 375px 宽单列布局
- **卡片内对齐**：名称左对齐、价格右对齐、meta 数据等宽分布
- **最大内容宽度**：375px（手机框）
- **圆角层级**：sm: 8px（按钮、输入框）、md: 12px（卡片）、full: 9999px（chip、标签）

## Motion

- **策略**：最小功能化
- **过渡**：150ms ease-out（tab 切换、下拉展开、chip 状态切换）
- **按下反馈**：`transform: scale(0.96-0.985)` + 轻微透明度变化
- **数值更新**：无动画，即时响应（房贷计算器）
- **页面切换**：小程序原生 `wx.navigateTo` 滑动动画

## Components

### 项目卡片（找房列表）
- 名称 + 装修标签（精装/毛坯）
- 位置（区 · 街道）
- 单价 + 总价区间 + 面积区间
- 套数（总量/剩余）
- 去化率进度条（6px 高，色彩按去化率自动切换）
- 取证日期 + 时效标签（今日新取证/已过N天）

### 筛选行（找房列表）
- 单行 5 个下拉触发器（户型/总价/面积/装修/排序）
- 已选条件摘要栏（浅绿底，每个 tag 可单独删除）
- 快速组合预设按钮（横向滚动，橙色系）

### 单元表格（楼盘详情）
- 5 列网格：楼层 · 房间号 · 备案总价 · 建筑面积 · 状态
- 状态标签：可售（绿）、已签认购（橙）、已备案（红）
- 楼栋/单元/状态/户型 筛选下拉

### 楼栋去化卡片（楼盘详情）
- 每栋一行：名称 · 单元数/总套数 · 进度条（8px） · 已售/总量 · 百分比
- 100% 去化用红色标记

## 历史成交对比

### 产品目标与页面层级

- **核心任务**：把同一个自然月在连续 3 年或 5 年的成交量放在一起，让用户无需心算即可判断今年同月相较往年增长还是下滑。
- **入口**：在“我的”页将“历史成交对比”放在“成交概览”下方，作为同组第二行，进入独立页面。
- **默认视图**：`月度同期`。`年度同期`和`连续走势`为二级视图，不得抢占默认入口。
- **一级分段**：`月度同期｜年度同期｜连续走势`，切换区域高度不得低于 44px。
- **首屏优先级**：同月跨年精确对照 > 年度累计判断 > 连续月份趋势。

### 公共筛选与年份定义

- 指标切换：`总成交｜新房｜二手房`，默认`总成交`；一次只展示一个指标，禁止在单张月份卡内同时绘制三套年份条形。
- 年限切换：`近3年｜近5年`，默认`近3年`。
- `近3年`始终表示今年、去年、前年，共 3 个年份。例如 2026 年对应 2026、2025、2024。
- `近5年`始终表示含今年在内连续 5 年。例如 2026 年对应 2026—2022。
- 筛选区同时显示实际年份范围，例如`2024—2026`，避免“近3年”产生歧义。
- 切换指标或年份后保留当前视图和滚动位置，筛选条件不得失忆。

### 月度同期（默认视图）

- 页面直接展示单张数据矩阵：月份为行、年份为列；月份按最新到最早排列，年份按新到旧排列。
- 表格上方仅保留一行筛选控件：`近3年｜近5年`与`总成交｜新房｜二手房`；不再使用独立筛选卡或“月度成交对照”标题。
- 每个单元格只显示当前选中的成交指标，精确数量常驻；缺失数据展示`—`，不得按 0 参与比较。
- 当前月份在月份行旁显示简短的`进行中`标识，不再设置独立“数据截止”提示卡。
- 近 5 年模式完整显示 5 个年份列，表格必须适配 320—430px 手机宽度，不触发页面横向滚动。

### 年度同期（二级视图）

- 所有年份统一统计`1月1日—数据截止日对应的同月同日`，禁止拿今年未完成年度与历史全年比较。
- 使用纵向年度账本；每个年份采用两层布局：首行展示年份与精确总成交，次行使用全宽堆叠条并标注新房、二手房数值。不得把全部字段压进同一行。
- 新房使用蓝色、二手房使用橙色，堆叠后的总长度表达总成交；精确总数始终右对齐显示。
- 今年相较历史同期均值的结论和排名可作为摘要，但不得取代逐年精确数据。

### 连续走势（二级视图）

- 默认时间范围为去年 1 月至今年当前月，标题必须显示明确范围，例如`2025.01—2026.08`。
- 图表只绘制新房、二手房两条线；总成交在选中月份的固定读数卡中展示，不增加第三条线。
- 横轴可稀疏显示月份标签，但底层必须保留逐月原始数据，不得擅自季度化、平滑或改成月均桶。
- 按住或拖动图表时吸附到最近月份；读数卡固定在图表上方或下方，避免被手指遮挡。
- 去年与今年交界处使用细分隔线和年份标签，帮助用户定位时间。

### 数据口径与状态

- 全页面使用接口返回的真实`latest_date`作为数据截止日，不得使用手机当前时间代替。
- 已结束月份按完整自然月统计；当前月份必须让所有年份统一截取到同一日，再计算跨年增减。
- 当前月份无法获得历史同日数据时，不做同比结论；显示`暂不提供同期判断`，不得与历史完整月直接比较。
- 当前月份同时使用文字“进行中”、明确截止日期和虚线/空心视觉标记。
- 缺失数据展示`—`或`数据不完整`，不得按 0 参与条形、总计或增减计算。
- `总成交 = 同口径新房 + 同口径二手房`；任一组成项缺失时，总成交不得伪造为完整值。
- “数据截至日”和“页面更新时间”是两个字段，必须分别标注。
- 1—2 月受春节日期错位影响时，可增加工作日差异提示；重要判断优先参考年度累计同期。

### 手机布局与可访问性

- 设计基准为 375—390px 单列手机视口，并验证 320px、375px、390px、430px 宽度。
- 页面左右留白 16px；禁止页面级横向滚动。只有明确标识的图表内部才允许横向拖动。
- 所有可点击区域至少 44×44px；分段控件、指标切换和年份切换应整格可点。
- 主数值使用 20—24px / 800；月份和年份使用 12—14px / 600—700；关键口径不得低于 12px / `#666`。
- 选中态同时使用颜色、字重和边框，不得仅依赖颜色。
- 图表必须提供等价文字摘要；缺失、进行中、上涨和下滑均应具备可朗读文本。
- 交互过渡统一使用 150ms ease-out；数值更新不使用滚动计数动画。

### 成交数据配色

| 角色 | 色值 | 用途 |
|------|------|------|
| 交互选中 | `#07C160` | 分段、指标、年份范围选中态；不得用于成交量数据 |
| 新房 | `#3B82F6` | 仅用于成交图表中的新房折线、条形、图例；不得扩大为普通组件颜色 |
| 二手房 | `#FF8C00` | 仅用于成交图表中的二手房折线、条形、图例；不得扩大为普通警告或组件颜色 |
| 总成交 / 今年 | `#1A1A1A` | 总成交主数值、当前年份条形与强调线 |
| 历史年份 | `#B8B8B8` / `#D8D8D8` | 去年及更早年份条形 |
| 未完结月份 | `#D97706` + `#FFF7E6` | 进行中提示、截止日和虚线边框 |

### 已确认设计资产

- 高保真 HTML：`~/.gstack/projects/beian_miniapp/designs/design-system-20260831/mobile-preview.html`
- 默认页为“月度同期”：同一月份的近 3 年或近 5 年数据纵向并列，融合条形图、精确值和相邻年份增减。

## Design Principles

1. **数据即界面** — 去化率不是装饰，是核心输入。颜色跟随数据语义。
2. **信息密度优先** — 一张卡片回答"值不值得点进去"，不翻第二屏。
3. **筛选条件永不失忆** — 已选摘要栏常驻，不可省略。
4. **列表看整体，详情看拆分** — 楼栋级数据进详情页，列表页只放项目级。
5. **零网络字体依赖** — 系统字体栈，适配小程序平台约束。

## Decisions Log

| 日期 | 决策 | 理由 |
|------|------|------|
| 2026-06-14 | 初始设计系统创建 | 基于竞品研究（贝壳/链家/安居客）+ 行业最佳实践 + 小程序平台约束 |
| 2026-06-14 | 楼栋去化从列表页移至详情页 tab | 列表页信息过载；详情页空间充裕，用户意图明确 |
| 2026-06-14 | 筛选行压缩为单行下拉 | 参考贝壳模式；4 行展开 chip 占用太多屏占比 |
| 2026-06-14 | 使用系统字体栈，不引入外部字体 | 微信小程序 `@font-face` 限制 |
| 2026-08-31 | 历史成交对比默认使用月度同期 | 用户核心任务是将同一月份的连续 3 年或 5 年精确数据直接对照；年度累计与连续走势降为二级视图 |
